version: '3.8'

services:
  pizza-api:
    build: .
    container_name: pizza-api
    restart: always
    env_file:
      - .env
    volumes:
      - ./orders.json:/app/orders.json
    ports:
      - "3001:3001"
